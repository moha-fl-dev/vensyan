import { businessDataAccess } from './business-data-access';

describe('businessDataAccess', () => {
  it('should work', () => {
    expect(businessDataAccess()).toEqual('business-data-access');
  });
});
